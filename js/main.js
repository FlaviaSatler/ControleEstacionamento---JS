(function () {
    const $ = q => document.querySelector(q);

    const register = $('#send');
    register.addEventListener("click", function(){
        const name = $('#name');
        const licence = $('#licence');

        alert("Aeee")
    })
})()